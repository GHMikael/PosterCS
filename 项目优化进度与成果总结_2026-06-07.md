# PosterCS / SVFP 项目优化进度与成果总结

> 汇总时间:2026-06-07
> 适用范围:近期(2026-06-04 ～ 06-07)围绕「优化方向、做了哪些东西、得到什么结论」的全部工作。
> 一句话:**项目从"做一个海报生成系统"正式转向"用 PosterCS 这个可控版面基座去验证 SVFP 这个通用视觉修复协议";围绕这个定位完成了失败 taxonomy 审计、一次关键的 text_overload 真实性调查、base 生成器打磨、SVFP 的 VLM 修复、以及 Dify→缓存 流水线。**

---

## 0. 先看这一张表(全局进度速览)

| 模块 | 状态 | 一句话结论 |
| --- | --- | --- |
| 论文定位重构(一主两从) | ✅ 已确定 | SVFP 是主贡献,PosterCS 是验证基座,不再硬碰 SOTA 生成质量 |
| P0 失败 taxonomy 审计 | ✅ 完成并通过 | 5 类 issue + 4 guard 经 48 张真实初稿数据驱动验证,三个硬门槛全过 |
| `text_overload` 真实性调查 | ✅ 完成(结论重大) | 现行标注下它近似"常量标签 / VLM 偏见";真正高频问题是**图表(asset)**,这反而**强化了 SVFP severity-gating 的动机** |
| Type A / Type B 三诊框架 | ✅ 已确立 | 用来判断"哪些缺陷该在 base 修、哪些留给 SVFP",解开"我改好质量 SVFP 还修什么"的纠结 |
| base 生成器打磨(base-v2) | ✅ 一轮完成 | 按 7 点修了 Type A 确定性缺陷;SVFP 冻结区一行未动 |
| SVFP 的 VLM 评论器 | ✅ 已修复(原本是坏的) | 之前**静默退化成启发式**,等于 SVFP 闭环空转;已修为 Qwen3-VL-32B + socksio |
| Dify→缓存 流水线 | ⏳ 进行中(20/64) | 直采 PlannerAgent JSON、可移植缓存已打通;figures:0 偏高待解决 |
| 16 指标 / baseline / judge 基础设施 | ✅ 代码就位 | 5 个核心 baseline + 2 个外部 SOTA 占位 + judges 齐全 |
| 新系统重审计(re-audit) | ⏳ 待启动 | 旧 48 张语料已**过时**,需在新系统上重跑审计才能定稿 taxonomy |

图例:✅ 完成 ⏳ 进行中 ⚠️ 风险/待决

---

## 1. 最重要的一件事:项目定位的根本转变

这是近期所有工作的总纲,记录在 `PROJECT_OPTIMIZATION_DIRECTION_v4.md` 与 `含金量分析评估.md` 两份文档里。

**旧定位(危险):**
> 我们做了一个从论文自动生成海报的多智能体系统。

**新定位(已确定):**
> **PosterCS 不是一个独立的 SOTA 海报生成器,而是一个可控的结构化版面基座(controllable structured layout substrate),用于验证 SVFP —— 一个把 VLM 视觉批评转化为可执行、可收敛、可审计的版面修复协议。**

为什么必须转?因为 **Paper2Poster / PosterAgent、PosterGen** 已经占了"paper→poster 多智能体 + VLM commenter loop"这个坑。如果论文写成"又一个生成系统",新颖性会被压住,审稿人会问"你和 Paper2Poster 的本质区别是什么"。

**切入点的转移**:不再比"谁生成的海报更好看",而是研究 **"VLM 的视觉反馈如何被可靠地执行"**(critique-to-action gap)。这把论文从"系统工程"抬升为"方法论文"。

### 1.1 一主两从贡献结构

```
主  贡献① SVFP:结构化视觉反馈协议
        把 VLM critique 约束成封闭 issue-action schema,
        经 deterministic applier 实现可执行、可收敛、可审计的修复

从  贡献② CS-Poster-30 benchmark        从  贡献③ CS 垂直实例化
        frozen snapshots + baseline 矩阵       六模块 domain prior + renderer,
        + 协议/结果双层评测                     作为 SVFP 的高密度真实验证场景
```

### 1.2 三个核心 claim(论文主张)

- **Claim 1(协议):** SVFP 让 VLM 反馈比自由文本更可执行、更收敛。→ 对应 C 类指标 + `ours_freeform` 对照。
- **Claim 2(修复):** 给定同一张初始海报,SVFP 在有界延迟内提升版面质量与可读性,且不破坏内容。→ 对应 B1/B2/D1 + same-initial repair。
- **Claim 3(泛化):** SVFP 是 planner-agnostic 的,能改进不同来源 planner 的输出。→ 对应 `gpt4o_zeroshot_svfp`、可选 `sota_svfp`。

### 1.3 指标的三层定位(不要 16 个指标平均用力)

| 层级 | 指标 | 回答的问题 | 论文角色 |
| --- | --- | --- | --- |
| 主层 | C 协议指标 | SVFP 是否比自由反馈更可执行、更收敛? | 核心创新证据 |
| 第二层 | B 视觉质量 + D 效率 | 修复后视觉是否更好?代价多少? | 修复有效性 + Pareto |
| 第三层 | A 内容保真 + E 人评/Quiz | 修复是否破坏内容?人类是否买账? | 安全约束 + 外部验证 |

---

## 2. 已完成的实证工作

### 2.1 P0 —— 失败 taxonomy 审计(✅ 完成并正式通过,2026-06-04)

**目的(给审稿人的弹药):** 证明新的 issue 分类是**数据驱动**的,不是"拍脑袋的 prompt engineering"。这是 P0 的存在意义,也是回应"风险二:taxonomy 不扎实会被当作 prompt engineering"的直接防御。

**做法:**
- 语料 = 48 张已有的 `iter_1.png` 初始渲染(来自 `poster_agent_backend/outputs/runs/*/`)。
- 用 **Qwen/Qwen3-VL-32B-Instruct** 自动打标(47 成功 + 1 vlm_error)。
- 产物落盘在 `experiments/results/audit_v1_old48/`:`labels/*.json`(48 个)、`analysis_issue_distribution.json`、`analysis_issue_mece_audit.json`。

**结论 —— 三个硬门槛全部通过:**

| 门槛 | 阈值 | 实测 | 是否通过 |
| --- | --- | --- | --- |
| Coverage(覆盖率) | ≥ 0.80 | **1.0** | ✅ |
| Other Rate(归不进任何类的比例) | ≤ 0.10 | **0.0** | ✅ |
| 旧 overlap+contrast 占比 | ≤ 0.25 | **0.126** | ✅(证明把它们降级为 guard 合理) |

**最终落地的新分类(已实现在 `experiments/audit/taxonomy.py`):**

- **5 类主 issue:** `space_imbalance`、`text_overload`、`visual_hierarchy_weak`、`asset_utilization_error`、`structure_alignment_error`(+ `other`/`none` 哨兵)。
- **4 个 guard(降级的低频硬约束,可规则检测):** `overlap_guard`、`contrast_guard`、`overflow_guard`、`crop_guard`。
- **12 个 deterministic action** + `ISSUE_TO_ACTIONS` 映射表。

**重要边界(P0 的硬约束,至今仍然生效):**
> 生产环境的 SVFP 代码必须**冻结** —— `app/vlm_commenter.py` 里旧的 4 类 `SVFPIssueType` enum、`FeedbackApplier` 一律不动。新的 5 类 taxonomy **只存在于独立的 `experiments/audit/` 包里**。把它提升进 `app/` 是 **P1**(后续阶段)。

---

### 2.2 `text_overload` 真实性大调查(✅ 完成,结论重大,2026-06-05)

这是近期**最有价值、也最曲折**的一条研究线。起点是一个直觉怀疑,终点是一个能改变论文叙事的结论。

#### 起因(用户提出的怀疑)
在 `analysis_issue_distribution.json` 里,`text_overload` 和 `visual_hierarchy_weak` 都是 **100% presence(47/47)**。一个在 100% 样本上都触发的类几乎没有区分力 —— 它描述的是渲染器输出的**常量属性**,而不是"这一张海报的诊断"。用户还对一张 BLIP 海报具体反驳:VLM 说它"文字过载",但用户读起来是"刚好/充实"。

#### 我们用四步逐层逼近真相(全部零额外 API 成本或本地计算):

**① 密度分组检验** (`analysis_text_overload_validity.py`)
- 按 VLM 判定的 primary 分组,比较字符密度代理。
- 结果:`text_overload`-primary vs `vhw`-primary 的 **AUC 仅 0.637**(mean_chars)/0.556(max_chars)/0.536(bullets)—— 接近抛硬币。
- 即:**现行标注下,`text_overload` 不是被密度区分出来的,而是一个近乎常量的标签。**
- 反转细节:那张 BLIP 其实在 mean_chars 上是 **83 百分位(客观偏密)**,但用户判它"刚好"。→ 字数 ≠ 感知过载,缺的变量是**渲染字号**。

**② 真实几何 + 字号挖掘** (`analysis_text_overload_geometry.py`,用 python-pptx 直接读每个 run 的 `iter_1.pptx`)
- 两个基建发现:(a) 字号/面积**现在就能从 pptx 还原**,不必等 P2 telemetry;(b) 计划里写的 `page_size='A3'` 是**错的**,实际是 33.9×19.1cm 的 16:9。
- 关键结论:
  - **渲染器把局部密度和字号锁死了** —— `chars/cm²` 各组中位数都 ≈ 6.5–7;`body_font` 中位 8.8pt、底 7.2pt,**全 47 张 std = 0**(没有 shrink-to-fit)。→ 局部密度/字号**不可能**是过载信号。
  - **只有 `n_content_boxes`(文本块数量)有点区分力**(AUC 0.782),但这**大半是模板混淆** —— 在同一 `template_classic` 内 AUC 掉到 0.703 且只有 n=3,差距仅 1 个 box,**不稳健**。
- 即:**没有任何自动代理能稳健地把 `text_overload` 和 `vhw` 区分开** → **人工校准是被逼出来的,不是可选项。**

**③ 人工金标(human gold)** (`build_human_gold_table.py` → 用户填 `human_gold_subset.csv` → `analysis_human_gold_kappa.py`)
- 16 张密度跨度 0–98 百分位的海报,用户逐张标注。
- **用户把 16 张全部标为 `human_overload = no`** —— 哪怕最密的那张(98 百分位)也判"文字不算多"。
- κ(human, VLM) on text_overload **presence = 0.0**,观察一致率 0.0(VLM 16/16 说有,人 0/16,完全对立)。
- **大反转:** 用户的真实主问题是**空间/图表**,不是文字。在 9/16 上,人说"空太多(bullet 间距松)",VLM 说"字太多"—— 对同一张海报的相反解读。

**④ 重新精标 + 决定性发现** (用户认真重标 `human_primary`)
- 人的真实 issue 分布:**`asset_utilization_error` 12/16(75%,就是"图")** + `overlap_guard` 2 + `structure_alignment_error` 1。
- VLM 的 primary:`text_overload` 11/16(69%)。
- **杀手级单元格:在 7/16 上,人看到的是"图的问题",VLM 却说"text_overload"** → **VLM 系统性地用 `text_overload` 顶替了图表问题。**

#### 这条调查的最终结论(对论文极其重要)

1. **`text_overload` 在现行标注下被证伪为"主导高频类"** —— 它更像 VLM 的反射性默认抱怨(prior),而非每张海报的真实诊断。
2. **但这不是"VLM 瞎了",而是"VLM 排错序(mis-ranking)"** —— 它把真正的图表/重叠问题埋在了一个反射性的 text_overload 标题之下。**而 mis-ranking / severity 校准正是 SVFP severity-gating 要解决的问题 —— 这反而强化了 SVFP 的动机。**
3. **人真正关心的是图表(asset_utilization_error)**,这验证了 taxonomy 里这一类是真的;只有 `text_overload` 的**主导地位**被推翻。
4. **诚实的细节(写论文要注意):** 两个标注者其实都是低方差先验 —— VLM 默认 text_overload,用户默认 space_imbalance/图。所以这不是"人能区分而 VLM 不能",而是"两个相反的偏见"。**唯一标注者还是系统作者本人(内部证据),审稿人会说'作者给自己打分'** → 下一步硬性需要**独立第二标注者**。

#### 这条线还触发了一个全局性的连锁结论(见 §6)
用户在**新版 PosterCS 里已经修好了审计中两个最主要的失败**(无关/错误配图、标题重叠)。→ **旧的 48 张审计语料相对于论文要评测的系统已经"过时(stale)"了,taxonomy 必须在新系统上重新接地。**

---

### 2.3 Type A / Type B 三诊框架(✅ 已确立)

用来解开用户反复纠结的一个问题:**"如果我把 base 生成质量改好了,那 SVFP 还修什么?"**

每个缺陷只问一句话:**它是否在每张海报上都以同样方式出现?**

| 类型 | 定义 | 处理 |
| --- | --- | --- |
| **Type A —— 确定性缺陷** | 由固定规则/bug 导致,与论文内容无关(硬编码字符串、固定高亮正则、固定行距、写死的 6 栏网格) | **必须在 base 生成器修**。让 SVFP 去"修"一个确定性 bug 是无效评测,而且这些 bug 会淹没真实信号 |
| **Type B —— 内容相关的版面失败** | 是否出现取决于具体论文的文字/图;没有固定规则能预先算出修法,只能 render→看→修 | **这是 SVFP 的领地**,无论 base 规则多好都不会消失 |

**关键反转(解纠结的那句话):** 修 Type A **反而磨利了 SVFP 的故事**。一旦 base 用上了最好的固定规则,任何残余失败就**可证明不是规则能修的**,只能靠"看了再修"。**"base 越干净,SVFP 越不可替代。"**

**陷阱(伪装成 base 打磨的 Type B 工作,绝不能写死):** 用关键词自动填白(会删掉 space_imbalance 这个失败模式)、为每个 panel 挑"完美"的 bullet 数/图大小(会删掉 asset/text 这个 substrate)。这些要留给 SVFP loop。

---

### 2.4 base 生成器打磨 base-v2(✅ 一轮完成,2026-06-05)

按用户提的 7 点,用 §2.3 的框架只修 Type A。改动经 render + LibreOffice 栅格化验证。

**`app/ppt_renderer.py`(已确认是生效的渲染器):**
- 删掉 classic 模板写死的副标题 "Classic academic poster layout…"。
- 重写 `KEYWORD_RE`:标识符内部的数字(如 G2ConS 里的 "2")不再被高亮;去掉裸整数;高亮色由 accent 软化为 primary。
- 给 `METRIC_RE` / 数据卡抽取加同样的标识符边界保护。
- `add_figure` 现在预留 caption 条,**caption 不再压在图上**。
- `add_bullets` 限制每行高度(`max_row_h = 0.56" × font-scale`),bullet 紧贴顶部而不是被拉满。
- dashboard/classic/minimal 模板按 panel 数(4/5/6/7)选**非对称 span 表**,不再是写死的 6 格。

**`dify/prompts/planneragent.txt`:** panel 数放宽到 4–7 并明确"别总输出 6 / 挑选·合并·省略";图 panel 的 bullet 由 3→1;修 `ture`→`true`。

**刻意没改(留给 SVFP / 属于设计):** `color_theme`、`max_iterations`(只是安全上限,收敛才是主停机)、`feedback_loop.py` / `vlm_commenter.py`(SVFP 冻结区)、未填的留白(它是 space_imbalance 的 substrate)。

**运维注意:** renderer 改动对服务即时生效;但 `planneragent.txt` 只是源文件,**Dify app 里有自己的一份 prompt 副本,必须在 Dify 里重新同步**才能让 prompt 改动生效。

---

### 2.5 SVFP 的 VLM 评论器:原来是坏的 → 已修复(✅ 2026-06-07,关乎研究诚信)

**严重问题:** 在 2026-06-07 之前,SVFP 的 VLM 评论器一直在**静默失败并退化成启发式版面检查器**(`run_report` 里 feedback 的 `source` 是 `ppt_screenshot_heuristic`,comment 写着 "VLM failed: ...")。**也就是说,SVFP 闭环的核心 —— 真正的 VLM 视觉批评 —— 是空转的。**

两个叠加的 bug:
1. `.env` 里 `QWEN_VL_MODEL=Qwen/Qwen2.5-VL-7B-Instruct` 在 SiliconFlow 上报 400 `Model does not exist`(整个 Qwen2.5-VL 系列已下架,只剩 **Qwen3-VL**:8B / 30B-A3B / 32B,各有 Instruct/Thinking)。
2. 修了模型后,`httpx`(openai SDK)用不了环境里的 SOCKS 代理,因为没装 `socksio`(`requests` 自带 socks 支持,这就是为什么早期裸 `requests` 探针能通、但 app 的 openai/httpx 路径失败)。

**已修复并端到端验证:**
- `QWEN_VL_MODEL=Qwen/Qwen3-VL-32B-Instruct` 写进 `.env`、`.env.example`、`app/config.py:16` 默认值(用户选 32B 求质量;避开 `-Thinking`,它会输出推理破坏闭合 JSON schema)。
- `socksio==1.0.0` 安装并钉进 `requirements.txt`。
- 验证 `check_layout_with_vlm` 返回 `source: vlm` 且给出真实批评(在一张稀疏测试海报上正确标出 empty_space)。

**⚠️ 研究诚信行动项:** 任何在"坏窗口期"产生的 `ours_svfp` / `gpt4o_zeroshot_svfp` / `ours_freeform` 结果都是**启发式、不是真 VLM 反馈**(Jun 1–2 的 zcache 旧 run 就是坏的)。**信任任何 B1/B2/C 类 SVFP 数字之前,先核对结果生成时间,并重跑受影响的部分。**

---

### 2.6 Dify → 缓存 流水线(⏳ 进行中,20/64,2026-06-07)

**目标:** 把 Dify 产出的 PosterTask JSON 直接灌进 `datasets/planner_cache/`。因为实验是从缓存的 plan 重渲染(`cached_plan()`),Dify 自己那次渲染是一次性的 —— **只需要 PlannerAgent 的 JSON**。

**已完成:**
- `experiments/scripts/dify_to_cache.py` 取代了旧的 batch/import 两步(不再靠标题匹配)。两条捕获路径:
  - `--capture stream`(默认,可并行 `--max-workers N`):从每个 Dify SSE 里抓 PosterTask。**2026-06-07 实测通过**。注意:task 是作为 JSON 字符串出现在 `workflow_finished` 里(约 115s,渲染后),没有更早的 planner 节点 → `--stop-after-capture` 无意义。
  - `--capture folder`(串行兜底):按 ctime 认领 `outputs/runs/<folder>` 读 input.json。也已端到端验证。
- **缓存可移植性已完成:** 两个写入器都剥掉 `image_source`/`image_url`/`thumbnail_url`,缓存只留 `asset_token + figure_id`,运行时再 `hydrate_task_image_sources()`。
- 建了 `experiments/configs/papers_all.json`(全 64 篇);`papers_30.json` 保留为精选基线。

**⏳ 待办 / 风险:**
- **已缓存 20/64**(用户在 20 处主动停下做检查点)。20 张结构都干净、可移植。
- ⚠️ **约 40%(8/20)抽出 0 张图** —— 很可能是 `app/pdf_assets.py` 的图过滤器(composite-figure / too_small<120px / tiny_on_page<1.5%)**过度丢弃**。有些是本该有图的方法/理论论文。这会**削弱 A2/figure_reuse 指标**,考虑在跑全量 64 + 实验前先修图过滤器。(这点在 `zcache/terminal.txt` 的批量日志里也能看到大量 `skipped images` 记录。)
- 缓存数据(`datasets/planner_cache/*.json`)**尚未提交**(代码已在 c64918d 提交)。
- 剩余 44 篇:`python -m experiments.scripts.dify_to_cache --limit 64 --skip-cached --max-workers N` 续跑。

---

### 2.7 实验基础设施(✅ 代码就位)

| 类别 | 现状 |
| --- | --- |
| **16 个正式指标** | `experiments/metrics/` 下 A(内容保真)、B(视觉质量)、C(协议)、D(效率)、E(人评/Quiz/LLM judge)五类齐全 |
| **5 个核心 baseline** | `ours_svfp`、`ours_no_svfp`、`ours_freeform`、`gpt4o_zeroshot`、`gpt4o_zeroshot_svfp` |
| **2 个外部 SOTA 占位** | `paper2poster.py`、`posteragent.py`(质量参照;无 feedback loop 的 C 类标 N/A) |
| **judges** | VLM layout judge、NLI、AltCLIP、PaperQuiz 生成/作答、claim 抽取、gpt4o planner、layout 几何 |
| **审计包** | `experiments/audit/`(taxonomy / vlm_labeler / corpus)+ 一系列 `analysis_*.py` 分析脚本 + `test_audit_taxonomy.py` |

---

## 3. 关键成果清单(我们到底"得到了什么")

**战略层面:**
1. 把论文从"易被压制的生成系统"重定位为"有明确方法论 spine 的协议论文(SVFP)",并写成可直接落地的 `PROJECT_OPTIMIZATION_DIRECTION_v4.md`。
2. 一主两从贡献结构 + 三个 claim + 指标三层定位 全部敲定。

**实证层面:**
3. P0 审计完成且三门槛全过 —— taxonomy 是 **data-driven** 的,有了正面的审稿人弹药。
4. `text_overload` 真实性被一套四步、零额外成本的分析**证伪为"主导类"**,同时把它**重新诠释为 mis-ranking 问题 → 反而强化了 SVFP severity-gating 的卖点**。这是一个"看似打脸、实则加分"的高质量负结果。
5. 发现真正的高频问题是**图表使用(asset_utilization_error)**,为 taxonomy 的重心调整(抬升 asset/space,把 text_overload 降为 severity-gated)提供了证据。

**工程层面:**
6. 修复了一个**会让全部 SVFP 结果作废的隐藏 bug**(VLM 静默退化为启发式)——这是关乎研究诚信的关键修复。
7. 打通了 Dify→可移植缓存 的直采流水线,把"Dify 渲染慢"与"实验只要 plan"解耦。
8. base-v2 在不污染 SVFP 评测的前提下,清掉了一批 Type A 确定性缺陷。

---

## 4. 当前状态快照(可以直接拿来对照)

- **生效渲染器:** `app/ppt_renderer.py`(`poster_agent_backend/app` 那份是死代码)。
- **VLM 模型:** `.env` 与 `app/config.py` 均为 `Qwen/Qwen3-VL-32B-Instruct`;`socksio` 已装。VLM 路径已验证可用。
- **SVFP 冻结区:** `app/vlm_commenter.py` 的 4 类 enum、`FeedbackApplier`、`feedback_loop.py` —— **未改,P0 约束仍生效**。
- **新 5 类 taxonomy:** 只在 `experiments/audit/taxonomy.py`(5 issue + 2 哨兵 + 4 guard + 12 action + 映射)。提升进 `app/` = P1,尚未做。
- **审计语料:** `experiments/results/audit_v1_old48/`(48 张旧初稿 + labels + 5 个 analysis JSON + human_gold_subset.csv + 接触表)。**标记为 v1_old48 = 已知过时。**
- **planner 缓存:** `datasets/planner_cache/` 20/64,可移植,**未提交**;约 40% figures:0 待查。
- **git:** 上一个提交 `c64918d`(Dify→cache 流水线 + 可移植缓存 + VLM 修复);大量审计/分析/baseline 文件仍是 untracked,缓存数据未提交。

---

## 5. 待决策 / 待办(按优先级)

### 待用户拍板的决策
1. **新系统 taxonomy 重审计的方式**(§6):(A) 先在新系统上重跑审计 [推荐] / (B) 先按直觉改 taxonomy 后补验证 / (C) 只把 text_overload 降级。**方法论警告:不要手改 taxonomy 去贴合直觉(="你调过了"的批评),要让新一轮数据来驱动。**
2. **独立第二标注者:** text_overload 调查的唯一标注者是系统作者本人,审稿人会质疑。需要找同门/导师当独立 human gold,和/或用跨模型 VLM 做自一致性。
3. **`structure_alignment_error` 去留:** 旧审计中 3/47、从不为 primary,是为 MECE 完整性保留还是并类。
4. **figure 过滤器是否在全量 64 之前先修**(影响 figures:0 率与 A2/figure_reuse)。

### 工程待办
- 续跑缓存 20→64,并提交缓存数据。
- 重跑坏窗口期的所有 SVFP 结果(VLM 修复后)。
- 在 Dify 后台同步 `planneragent.txt` 的新 prompt。

### 路线图(来自 v4 文档,P0 之后)
- **P1:** 把新 5 类 schema/prompt 升进生产 SVFP(改 VLM 输出类别 + action mapping)。
- **P2:** 补 renderer telemetry(`render_state.json` / `svfp_trace.json`)。
- **P3:** 实现 `c3_issue_resolution_rate`(新版 C 类里最值钱的硬指标)。
- **P4:** old-schema vs new-schema 小规模 ablation(证明不只是改名)。
- **P5/P6:** same-initial repair 实验 → n=30 正式实验(BH-FDR、效应量、置信区间)。
- **P7:** 外部 SOTA 复现 + 可选 `sota_svfp` 后处理验证。

---

## 6. 一条贯穿性的重大结论:旧审计语料已"过时"

这是 §2.2 调查的副产品,但影响全局,单独拎出来:

> 用户在**新版 PosterCS** 里**已经修好了**审计中两个最主要的失败 —— 无关/错误的配图(asset 抽取代码改进)和标题重叠。

因此:
- **P0 用的旧 48 张 `iter_1`(`poster_agent_backend`)对论文要评测的系统已经不具代表性。**
- taxonomy 必须**在新系统上重新接地**:生成一批新海报 → 重新打标 → 看分布是否符合"预注册假设":
  > "新系统里 `text_overload` 和 `visual_hierarchy_weak` 大幅下降;`asset_utilization_error`、`space_imbalance`、`structure_alignment_error` 成为主导。"
- 用户预测的新系统失败(缺关键图、图太小、bullet 松、局部空、panel 错位)只映射到 3 个已有类;更细的子类型应该是 **SVFP 的修复 action(放大/插入/替换),而不是新的 issue 类**(保持 taxonomy MECE)。
- **必须定义"冻结点":taxonomy 在 系统版本-Y、n=Z 上验证。** 编辑 `experiments/audit/taxonomy.py` 属审计包范围(允许);`app/` enum 在 P1 前保持冻结。

---

## 7. 风险与审稿人防御(来自《含金量分析评估》)

| 风险 | 防御 |
| --- | --- |
| 被当作 Paper2Poster/PosterGen 的增量 | 把差异写硬:新意在**协议层**(closed executable schema + 协议级指标),不在 loop 本身 |
| taxonomy 像 prompt engineering | 用 P0 审计 + 分布/MECE/other-rate 撑起 data-driven(✅ 已做);新系统重审计巩固 |
| renderer 太弱,SVFP 像在"修自家 bug" | `ours_no_svfp` 当合理 baseline + `gpt4o_zeroshot_svfp` + same-initial repair + 不声称 renderer 全面超 SOTA |
| 16 指标平铺显乱 | 三层定位 + 实验拆成 3 张表(协议 / same-initial 修复 / 外部 SOTA 参照) |
| n=30 统计不够强 | 主 claim 押在效应最大的指标(C1/C2/C3、B1/B2、E1/E2),弱指标不做主 claim |

**最终判断(含金量):** 若按现路线做实(SVFP 主线 + substrate 定位 + 5 类 MECE taxonomy + 硬 C 指标 + free-form 强对比 + n=30 + PaperQuiz/人评 + 至少一个 planner-agnostic 验证),含金量为**中高**,具冲击好会议的潜力。成败关键不在 renderer 多漂亮,而在能否把这句话用实验完整撑住:
> **SVFP turns VLM visual critiques into executable, convergent, and transferable layout repairs.**

---

## 附录:关键文件地图

| 文件/目录 | 作用 |
| --- | --- |
| `PROJECT_OPTIMIZATION_DIRECTION_v4.md` | 优化方向总纲(定位、taxonomy、指标、路线图 P0–P7) |
| `含金量分析评估.md` | 选题价值/新颖性/风险/审稿人防御判断 |
| `experiments/audit/taxonomy.py` | 新 5 类 issue + 4 guard + 12 action(canonical) |
| `experiments/results/audit_v1_old48/` | P0 审计全部产物(labels / analysis JSON / human gold) —— **已过时标记** |
| `experiments/scripts/analysis_text_overload_*.py` | text_overload 真实性调查的三个分析脚本 |
| `experiments/scripts/analysis_human_gold_kappa.py` | 人机一致性 κ 计算 |
| `experiments/scripts/dify_to_cache.py` | Dify→可移植缓存 直采流水线 |
| `experiments/metrics/` | 16 个正式指标 |
| `experiments/baselines/` | 5 核心 baseline + 2 外部 SOTA 占位 |
| `app/ppt_renderer.py` | 生效渲染器(base-v2 改动在此) |
| `app/vlm_commenter.py` / `app/feedback_loop.py` | SVFP 生产闭环(**冻结区**) |
| `.env` / `app/config.py` | VLM 模型配置(已修为 Qwen3-VL-32B) |
| `datasets/planner_cache/` | PlannerAgent JSON 缓存(20/64,可移植,未提交) |

---

*本总结由 Claude 基于近期对话记忆 + 项目实际文件状态(2026-06-07)核对生成,供快速回顾。具体数字以对应 JSON/脚本输出为准。*
