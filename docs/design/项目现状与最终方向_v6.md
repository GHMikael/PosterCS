# PosterCS 项目现状与最终方向 v6（2026-06-09）

> 接续 `PROJECT_OPTIMIZATION_DIRECTION_v4.md`（方向总纲）、`SVFP_ISSUE_TAXONOMY_v5.md`（taxonomy/路由）、
> `项目优化进度与成果总结_2026-06-07.md`（上一阶段进度）。本文是**当前真实状态 + 先出一个能用 v1 + v1 之后的最终方向**。

## 0. 一句话
> 生成与评测两条链已基本打通、质量明显改善,**可以先冻结一个"能用的 v1"**;论文最深的那块——**把生产 SVFP 闭环从"旧 4 类 + 整图 VLM"迁移到"5 类 + 按问题路由检测 + 严重性门控"——设计好了但还没进生产**,这是 v1 之后的核心研究任务。

---

## 1. 现状总结:这一轮做了什么

| 模块 | 状态 | 说明 / 提交 |
| --- | --- | --- |
| **诊断(论文主线证据)** | ✅ | VLM critic 不可靠**非小模型问题**(8B/30B/32B 全先验主导);窄提示词也只把 asset 召回提到 4/12;**c3=0.0**(SVFP 一轮没解决一个 issue)。三条都是可报告的硬证据 |
| **图表抽取** | ✅ | fitz→**docling**(矢量图+表都能抽,fitz 漏的),主路+fitz 兜底,`POSTER_USE_DOCLING` 可关 (`6cebea9`) |
| **16 指标重构** | ✅ | 新命名落地(A/B 改名 + a3 BERTScore + e2/e3 新增,删 5 废弃);17 注册、测试零报错 (`bcae792`) |
| **协议指标 C 类** | ✅ | c1 **honest**(applier 真实计数,不再写死 1.0)、c2、**c3 issue_resolution**(新建)、c4 (`d08183a`,`2b59861`) |
| **内容自适应布局** | ✅ | `content_spans` 替死六格(dashboard/classic/minimal);同 6 panel 不同内容→不同几何 (`2dd573a`) |
| **headline 视觉焦点** | ✅ | Panel.headline + 渲染焦点行(dashboard/classic/minimal)(`2dd573a`,`80b6c00`) |
| **图布局按长宽比** | ✅ | 渲染器按图真实长宽比定方向(竖图左右/宽方图上下)+ 框贴图比例;planner §8 简化为 text_only/with_figure (`0be0761`) |
| **架构原则** | ✅ | **几何可判→确定性渲染器/规则;语义内容→LLM/VLM**(贯穿 docling、c3、图布局) |

8 个提交已全部 push 到 `origin/main`(到 `0be0761`)。

---

## 2. 对照方向文档:做到哪 / 没做

对照 v4 §11 路线图 P0–P7:

| 路线项 | 计划 | 现状 |
| --- | --- | --- |
| P0 失败 taxonomy 审计 | ✅ | 已完成(旧 48 张语料,三门槛全过)——但**语料已过时**,需在新系统重审计 |
| **P1 把新 5 类 schema 升进生产 SVFP** | ⏳ **未做** | **生产闭环仍是旧 4 类**(`app/vlm_commenter.py`);新 5 类只在 `experiments/audit/`。**这是最大的未建件** |
| P2 新 issue → action 映射 | ⏳ 未做 | action 仍是旧 9 个浅动作;路由检测(几何/脚本+LLM/VLM)未进闭环 |
| P3 renderer telemetry | 🟡 半做 | `svfp_trace` 已由 baseline 从 history reshape 出来(够 c3 用);`render_state.json`(逐元素 bbox/occupancy)未做 |
| P4 更新 C 类指标 | ✅ | c1 honest + c3 新建完成 |
| P5 same-initial repair 实验 | ⏳ 未做 | |
| P6 n=30 正式实验 | ⏳ 未做 | 内容指标重算 + BH-FDR + 效应量 + CI 都没跑 |
| P7 外部 SOTA 复现 | 🟡 占位 | `paper2poster.py`/`posteragent.py` 在,但未确认能复现 |

对照 v5 / 本轮新增设计:

| 设计 | 现状 |
| --- | --- |
| 图布局按长宽比(几何) | ✅ 已进渲染器 |
| 16 指标 + honest c1 + c3 | ✅ |
| **5 类路由检测(几何/脚本+LLM/VLM 分工)** | ⏳ **只在 v5 文档,未进生产闭环** |
| 严重性门控(severity-gating) | ⏳ 未做 |
| 第二独立标注者 / 前沿模型对照 | ⏳ 未做(诊断主张的硬需求) |

---

## 3. 一个必须正视的事实:生产闭环 ≠ 论文愿景

- **论文/v5 讲的是**:5 类 MECE issue + 按问题类型路由检测(几何管空间/结构/溢出、脚本+文本LLM 管图文、VLM 只管层级)+ 严重性门控 + 确定性修复。
- **生产闭环现在做的是**:旧 4 类(overlapping/empty_space/low_contrast/figure_too_small)+ 整图 VLM critic + 9 个浅动作。
- 所以 **c3=0.0 是在"旧闭环"上测的**——它证明了"旧式整图 VLM + 浅动作修不动真问题",**正好是迁移到 v5 路由设计的动机**,但 v5 设计本身还没实测过。

> 这不矛盾,反而是叙事:**v1(现状闭环)= baseline / 反例;v2(v5 路由设计)= 改进。** 但要在论文里说清"现在测的是哪一版"。

---

## 4. "能用的 v1":定义 + 验收清单(先冻结一个能用的)

**v1 的范围**:把**生成 + 评测**这两条已经成熟的链冻结成一个能用、可复现、不在改的版本。SVFP 闭环**就用现状旧 4 类**(它能跑、能出 c1/c2/c3/c4 数,作为 baseline 完全够)。**不**把 v5 路由重构塞进 v1。

**验收清单(把这几项关掉就能打 tag v1.0):**
- [x] 生成链:docling 抽取 + Dify 规划 + 自适应渲染(headline / 图按长宽比)端到端可跑、出图质量可用。
- [ ] **同步 v2 prompt 到 Dify**(你做):§8 方向简化 + bullet 数收紧 + headline 禁裸数字。
- [ ] **跑一次小批量(5–10 篇)冒烟**:确认新生成链稳定 + 产出 honest c1 / c3 真实数(现有旧 cell 的 c1 还是写死值)。
- [x] 评测链:16 指标 + `compute_metrics` + baseline 矩阵可跑(已 smoke 过)。
- [ ] **清理工作树**:决定 base-v2(README/planneragent.txt/工具删除)与 23 个未追踪产物(audit/results/planner_cache/中文文档)的去留并提交。
- [ ] 打 `git tag v1.0` + 在 README 写"v1 能力边界"(生成+评测可用;SVFP 为旧 4 类 baseline)。

**结论:离"能用的 v1"很近——主要差"同步 Dify + 一次冒烟批量 + 清理提交打 tag"。生成质量这一轮已经到位。**

---

## 5. v1 之后的最终研究方向(优先级)

1. **P1 生产闭环迁移到 v5(论文深度的核心)**:把 `vlm_commenter` 的 4 类 enum + applier 升级为 5 类 + **路由检测**(几何检测 space/structure/overflow;脚本+caption+文本LLM 检测 asset 图文不匹配;VLM 只判 hierarchy)+ 严重性门控。做完跑 **old-schema vs new-schema 小消融**(证明不只是改名,c3/质量真的更好)。
2. **新系统重审计 + 第二标注者 + 一个前沿模型(GPT-4o/Gemini)对照**:撑起"诊断 VLM critic 不可靠"的主张(现有唯一标注者是作者本人、只测了 Qwen 一家)。
3. **P6 n=30 正式实验**:三张表(协议 / same-initial 修复 / 外部 SOTA 参照)+ BH-FDR + 效应量 + CI;主 claim 押 c1/c3/b1/b2/E1·E2。
4. **P7 外部 SOTA 复现 + 可选 `sota_svfp`**:证明 SVFP 是可迁移的 post-hoc 协议(不依赖自家 renderer)。
5. 渲染细节继续打磨(如近方形图在宽矮格的尺寸、图重 panel 给更合适的格子)。

---

## 6. 论文叙事(锚定,不变)
> **SVFP turns VLM visual critiques into executable, convergent, transferable layout repairs** —— 但更锋利的切入是 **"VLM 版面 critic 系统性不可靠(已用规模消融 + c3=0.0 量化),因此可靠的修复必须按问题类型路由检测、把几何可判的交给确定性规则"**。v1 是现状 baseline,v2(路由设计)是改进,二者对照本身就是论文的核心实验。

*本文由 Claude 基于 2026-06-09 代码与提交实际状态核对生成。*
